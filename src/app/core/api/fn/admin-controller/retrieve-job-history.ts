/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { JobDetailFilter } from '../../models/job-detail-filter';
import { SuccessResponsePaginateJobDetail } from '../../models/success-response-paginate-job-detail';

export interface RetrieveJobHistory$Params {
  pageIndex?: number;
  pageSize?: number;
  filter: JobDetailFilter;
  'Correlation-Id'?: string;
}

export function retrieveJobHistory(http: HttpClient, rootUrl: string, params: RetrieveJobHistory$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponsePaginateJobDetail>> {
  const rb = new RequestBuilder(rootUrl, retrieveJobHistory.PATH, 'get');
  if (params) {
    rb.query('pageIndex', params.pageIndex, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('filter', params.filter, {});
    rb.header('Correlation-Id', params['Correlation-Id'], {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponsePaginateJobDetail>;
    })
  );
}

retrieveJobHistory.PATH = '/api/admin/jobs/list';
