/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ExpenseDetailFilter } from '../../models/expense-detail-filter';
import { SuccessResponsePaginateExpenseDetail } from '../../models/success-response-paginate-expense-detail';

export interface GetMyExpenses$Params {
  pageIndex?: number;
  pageSize?: number;
  filter: ExpenseDetailFilter;
  'Correlation-Id'?: string;
}

export function getMyExpenses(http: HttpClient, rootUrl: string, params: GetMyExpenses$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponsePaginateExpenseDetail>> {
  const rb = new RequestBuilder(rootUrl, getMyExpenses.PATH, 'get');
  if (params) {
    rb.query('pageIndex', params.pageIndex, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('filter', params.filter, {});
    rb.header('Correlation-Id', params['Correlation-Id'], {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponsePaginateExpenseDetail>;
    })
  );
}

getMyExpenses.PATH = '/api/account/expense/list/self';
