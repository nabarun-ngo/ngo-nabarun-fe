/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateActivityDto } from '../../models/create-activity-dto';
import { SuccessResponseActivityDetailDto } from '../../models/success-response-activity-detail-dto';

export interface CreateActivity$Params {
  id: string;
      body: CreateActivityDto
}

export function createActivity(http: HttpClient, rootUrl: string, params: CreateActivity$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponseActivityDetailDto>> {
  const rb = new RequestBuilder(rootUrl, createActivity.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponseActivityDetailDto>;
    })
  );
}

createActivity.PATH = '/api/projects/{id}/activity';
