/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SuccessResponseActivityDetailDto } from '../../models/success-response-activity-detail-dto';
import { UpdateActivityDto } from '../../models/update-activity-dto';

export interface UpdateActivity$Params {
  id: string;
  activityId: string;
      body: UpdateActivityDto
}

export function updateActivity(http: HttpClient, rootUrl: string, params: UpdateActivity$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponseActivityDetailDto>> {
  const rb = new RequestBuilder(rootUrl, updateActivity.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('activityId', params.activityId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponseActivityDetailDto>;
    })
  );
}

updateActivity.PATH = '/api/projects/{id}/activity/{activityId}';
