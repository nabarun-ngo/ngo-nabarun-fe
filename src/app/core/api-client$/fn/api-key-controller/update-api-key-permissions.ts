/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SuccessResponseApiKeyDto } from '../../models/success-response-api-key-dto';

export interface UpdateApiKeyPermissions$Params {
  id: string;
      body: Array<string>
}

export function updateApiKeyPermissions(http: HttpClient, rootUrl: string, params: UpdateApiKeyPermissions$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponseApiKeyDto>> {
  const rb = new RequestBuilder(rootUrl, updateApiKeyPermissions.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponseApiKeyDto>;
    })
  );
}

updateApiKeyPermissions.PATH = '/api/auth/apikey/permissions/{id}';
