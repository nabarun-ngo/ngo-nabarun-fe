/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SuccessResponsePagedResultUserFcmTokensDto } from '../../models/success-response-paged-result-user-fcm-tokens-dto';

export interface GetFcmTokensMetadata$Params {
  pageIndex: number;
  pageSize: number;
  userId?: string;

/**
 * Device type (WEB, ANDROID, IOS)
 */
  deviceType?: string;
  isActive?: 'Y' | 'N';

/**
 * Search string for device name, user name, or email
 */
  search?: string;
}

export function getFcmTokensMetadata(http: HttpClient, rootUrl: string, params: GetFcmTokensMetadata$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponsePagedResultUserFcmTokensDto>> {
  const rb = new RequestBuilder(rootUrl, getFcmTokensMetadata.PATH, 'get');
  if (params) {
    rb.query('pageIndex', params.pageIndex, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('userId', params.userId, {});
    rb.query('deviceType', params.deviceType, {});
    rb.query('isActive', params.isActive, {});
    rb.query('search', params.search, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponsePagedResultUserFcmTokensDto>;
    })
  );
}

getFcmTokensMetadata.PATH = '/api/notifications/fcm-tokens/metadata';
