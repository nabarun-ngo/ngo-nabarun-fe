/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SuccessResponsePagedResultTransactionDetailDto } from '../../models/success-response-paged-result-transaction-detail-dto';

export interface ListSelfAccountTransactions$Params {
  id: string;
  pageIndex: number;
  pageSize: number;
  txnId?: string;
  txnType?: Array<'IN' | 'OUT'>;
  txnStatus?: Array<'SUCCESS' | 'REVERSED'>;
  transactionRef?: string;
  startDate?: string;
  endDate?: string;
}

export function listSelfAccountTransactions(http: HttpClient, rootUrl: string, params: ListSelfAccountTransactions$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponsePagedResultTransactionDetailDto>> {
  const rb = new RequestBuilder(rootUrl, listSelfAccountTransactions.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('pageIndex', params.pageIndex, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('txnId', params.txnId, {});
    rb.query('txnType', params.txnType, {});
    rb.query('txnStatus', params.txnStatus, {});
    rb.query('transactionRef', params.transactionRef, {});
    rb.query('startDate', params.startDate, {});
    rb.query('endDate', params.endDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponsePagedResultTransactionDetailDto>;
    })
  );
}

listSelfAccountTransactions.PATH = '/api/account/{id}/transactions/me';
