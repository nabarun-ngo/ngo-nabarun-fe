/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SuccessResponseAccountDetailDto } from '../../models/success-response-account-detail-dto';
import { UpdateAccountSelfDto } from '../../models/update-account-self-dto';

export interface UpdateSelf$Params {
  id: string;
      body: UpdateAccountSelfDto
}

export function updateSelf(http: HttpClient, rootUrl: string, params: UpdateSelf$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponseAccountDetailDto>> {
  const rb = new RequestBuilder(rootUrl, updateSelf.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponseAccountDetailDto>;
    })
  );
}

updateSelf.PATH = '/api/account/{id}/update/me';
