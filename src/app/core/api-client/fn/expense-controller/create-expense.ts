/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateExpenseDto } from '../../models/create-expense-dto';
import { SuccessResponseExpenseDetailDto } from '../../models/success-response-expense-detail-dto';

export interface CreateExpense$Params {
      body: CreateExpenseDto
}

export function createExpense(http: HttpClient, rootUrl: string, params: CreateExpense$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponseExpenseDetailDto>> {
  const rb = new RequestBuilder(rootUrl, createExpense.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponseExpenseDetailDto>;
    })
  );
}

createExpense.PATH = '/api/expense/create';
