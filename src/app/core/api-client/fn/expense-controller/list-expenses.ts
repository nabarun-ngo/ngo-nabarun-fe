/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SuccessResponsePagedResultExpenseDetailDto } from '../../models/success-response-paged-result-expense-detail-dto';

export interface ListExpenses$Params {
  pageIndex: number;
  pageSize: number;
  startDate?: string;
  endDate?: string;
  expenseRefId?: string;
  expenseId?: string;
  expenseStatus?: Array<'DRAFT' | 'SUBMITTED' | 'FINALIZED' | 'SETTLED' | 'REJECTED'>;
  payerId?: string;
}

export function listExpenses(http: HttpClient, rootUrl: string, params: ListExpenses$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponsePagedResultExpenseDetailDto>> {
  const rb = new RequestBuilder(rootUrl, listExpenses.PATH, 'get');
  if (params) {
    rb.query('pageIndex', params.pageIndex, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('startDate', params.startDate, {});
    rb.query('endDate', params.endDate, {});
    rb.query('expenseRefId', params.expenseRefId, {});
    rb.query('expenseId', params.expenseId, {});
    rb.query('expenseStatus', params.expenseStatus, {});
    rb.query('payerId', params.payerId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponsePagedResultExpenseDetailDto>;
    })
  );
}

listExpenses.PATH = '/api/expense/list';
