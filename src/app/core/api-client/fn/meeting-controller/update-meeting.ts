/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SuccessResponseMeetingDto } from '../../models/success-response-meeting-dto';
import { UpdateEventDto } from '../../models/update-event-dto';

export interface UpdateMeeting$Params {
  id: string;
      body: UpdateEventDto
}

export function updateMeeting(http: HttpClient, rootUrl: string, params: UpdateMeeting$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponseMeetingDto>> {
  const rb = new RequestBuilder(rootUrl, updateMeeting.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponseMeetingDto>;
    })
  );
}

updateMeeting.PATH = '/api/communication/meeting/update/{id}';
