/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateMeetingDto } from '../../models/create-meeting-dto';
import { SuccessResponseMeetingDto } from '../../models/success-response-meeting-dto';

export interface CreateMeeting$Params {
      body: CreateMeetingDto
}

export function createMeeting(http: HttpClient, rootUrl: string, params: CreateMeeting$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponseMeetingDto>> {
  const rb = new RequestBuilder(rootUrl, createMeeting.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponseMeetingDto>;
    })
  );
}

createMeeting.PATH = '/api/communication/meeting/create';
