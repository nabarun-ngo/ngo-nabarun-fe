/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SuccessResponseWorkflowInstanceDto } from '../../models/success-response-workflow-instance-dto';

export interface CancelWorkflow$Params {
  id: string;

/**
 * Reason for cancellation
 */
  reason: string;
}

export function cancelWorkflow(http: HttpClient, rootUrl: string, params: CancelWorkflow$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponseWorkflowInstanceDto>> {
  const rb = new RequestBuilder(rootUrl, cancelWorkflow.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('reason', params.reason, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponseWorkflowInstanceDto>;
    })
  );
}

cancelWorkflow.PATH = '/api/workflows/{id}/cancel';
