/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateDonationDto } from '../../models/create-donation-dto';
import { SuccessResponseDonationDto } from '../../models/success-response-donation-dto';

export interface CreateDonation$Params {
      body: CreateDonationDto
}

export function createDonation(http: HttpClient, rootUrl: string, params: CreateDonation$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponseDonationDto>> {
  const rb = new RequestBuilder(rootUrl, createDonation.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponseDonationDto>;
    })
  );
}

createDonation.PATH = '/api/donation/create';
