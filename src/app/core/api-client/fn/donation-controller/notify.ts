/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProcessDonationPaymentDto } from '../../models/process-donation-payment-dto';
import { SuccessResponseDonationDto } from '../../models/success-response-donation-dto';

export interface Notify$Params {
  id: string;
      body: ProcessDonationPaymentDto
}

export function notify(http: HttpClient, rootUrl: string, params: Notify$Params, context?: HttpContext): Observable<StrictHttpResponse<SuccessResponseDonationDto>> {
  const rb = new RequestBuilder(rootUrl, notify.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SuccessResponseDonationDto>;
    })
  );
}

notify.PATH = '/api/donation/{id}/notify';
