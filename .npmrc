# Scoped packages publish to / install from the public npm registry (no token for install).
# Publisher: run `npm login` once (needs access to the @nabarun-ngo org on npmjs.com).
@nabarun-ngo:registry=https://registry.npmjs.org/

# Previous private GitHub Packages setup (keep for reference):
# @nabarun-ngo:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${NPM_TOKEN}
