import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ngo.nabarun.app.intr',
  appName: 'NabarunNGO',
  webDir: 'dist/ngo-nabarun-fe'
};

export default config;
